# Solid Queue recurring tasks (loaded by the worker in production).
production:
  prune_coaching_decisions:
    class: CoachingDecisionPruneJob
    schedule: "every day at 4am"
    # No-ops unless COACHING_DECISION_RETENTION_DAYS is set, so it is safe to
    # always schedule.
